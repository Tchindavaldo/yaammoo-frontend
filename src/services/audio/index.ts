/**
 * Point d'entree audio. `Audio` reproduit l'API `expo-av` retiree du SDK 57
 * (voir `legacyAudio.ts`) : `import { Audio } from "@/src/services/audio"`.
 */
export * as Audio from "./legacyAudio";
